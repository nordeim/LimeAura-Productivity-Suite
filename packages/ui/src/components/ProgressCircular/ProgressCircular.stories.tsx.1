/**
 * @file packages/ui/src/components/ProgressCircular/ProgressCircular.stories.tsx
 * @purpose Storybook stories for the ProgressCircular component.
 * @interface Storybook stories
 * @phase 5
 */
import type { Meta, StoryObj } from '@storybook/react';
import { ProgressCircular } from './ProgressCircular';

const meta = {
  title: 'Molecules/ProgressCircular',
  component: ProgressCircular,
  parameters: {
    layout: 'centered',
  },
  tags: ['autodocs'],
  argTypes: {
    value: { control: { type: 'range', min: 0, max: 100 } },
    size: { control: 'number' },
    strokeWidth: { control: 'number' },
    showLabel: { control: 'boolean' },
    label: { control: 'text' },
  },
} satisfies Meta<typeof ProgressCircular>;

export default meta;
type Story = StoryObj<typeof meta>;

export const Default: Story = {
  args: {
    value: 75,
  },
};

export const Large: Story = {
  args: {
    value: 40,
    size: 100,
    strokeWidth: 8,
  },
};

export const CustomLabel: Story = {
  args: {
    value: 60,
    label: '6/10',
    size: 60,
  },
};
