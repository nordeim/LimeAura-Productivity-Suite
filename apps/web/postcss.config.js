/**
 * @file apps/web/postcss.config.js
 * @purpose PostCSS configuration.
 * @phase 7
 */
export default {
  plugins: {
    // We will add plugins like autoprefixer later if needed.
    // CSS Modules are handled by Vite's config.
  },
};
